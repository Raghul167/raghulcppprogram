#include<iostream>
using namespace std;
int main()
{
    string n;
    int ns,a,is,x;
    cin>>n>>ns>>a>>is;
    x=is*a;
    cout<<"Amount for "<<is<<" "<<n<<" is "<<x<<endl;
    cout<<"Remaining number of pencil present in stock is "<<ns-is;
    return 0;
}
