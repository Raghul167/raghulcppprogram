#include<iostream>
using namespace std;
int fact(int n)
{
    int sum=0;
    for(int i=0;i<=n;i++)
    {
        sum=sum+i;
    }
    return sum;
}
int main()
{
    int n;
    cin>>n;
    cout<<"Sum of n numbers:"<<endl<<fact(n);
}
