#include<iostream>
using namespace std;
int main()
{
    int n1,n2,i,j,sum=0,add=0;
    cin>>n1;
    int a[n1];
    for(i=0;i<n1;i++)
    {
        cin>>a[i];
        sum=sum+a[i];
    }
    cin>>n2;
    int b[n2];
    for(j=0;j<n2;j++)
    {
        cin>>b[j];
        add=add+b[j];
    }
    if(n1=n2 && sum==add)
    {
        cout<<"same";
    }
    else
    {
        cout<<"Not Same";
    }
}
